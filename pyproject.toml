[project]
name = "pse-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.4",
    "jupyter>=1.1.1",
    "lab>=8.9",
    "pandas>=3.0.3",
    "pyarrow>=24.0.0",
]
